# Claude project instructions

Read `AGENTS.md` first. It is the canonical project guide shared with Codex.
Then read only the task-specific files selected by
`scripts/Get-AgentContext.ps1 -Area <area>`; do not load the complete live-test
history or all historical phase prompts unless the task specifically requires
them.
